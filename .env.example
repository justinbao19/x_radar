# Claude API Configuration
LLM_API_URL=https://api.anthropic.com/v1/messages
LLM_API_KEY=sk-ant-api03-xxxx
LLM_MODEL=claude-sonnet-4-20250514

# Optional: Set to 'true' to run browser in headless mode
PLAYWRIGHT_HEADLESS=false

# Anti-Detection Configuration
# Maximum number of sources to scrape per run (default: 8)
MAX_SOURCES=8

# Sampling mode: 'random' (recommended) or 'all'
# 'random' - randomly select MAX_SOURCES queries each run
# 'all' - scrape all queries (higher detection risk)
SAMPLING_MODE=random
